# Slim requirements for the hosted deployment (EMBEDDING_PROVIDER=gemini).
# Deliberately omits sentence-transformers/torch — the deployed instance uses
# Gemini's hosted embedding API, so the ~1GB torch stack is dead weight that
# wouldn't fit a free 512MB tier anyway. Local dev still uses requirements.txt
# for the local embedding option.
anthropic==0.117.0
fastapi==0.139.2
google-genai==2.12.1
openai==2.46.0
pgvector==0.5.0
psycopg-binary==3.3.4
psycopg==3.3.4
python-dotenv==1.2.2
rank-bm25==0.2.2
tree-sitter-language-pack==1.12.5
tree-sitter==0.26.0
uvicorn==0.51.0
